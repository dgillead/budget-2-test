require_relative "../spec_helper"

describe Budget do
  let!(:budget) { Budget.create!(9000) }
# expect every budget to have a limit
  
# expect the limit to be decreased

# expect see_current_limit to return the current limit

# expect set_budget_limit to set the limit
end
