require_relative "../spec_helper"

describe Activity do
  let!(:activity) { Activity.create! }
# expect every transaction to have a category associated with it


# expect every transaction to decrease the limit for the category it is associated with

# expect every transaction to decrease the overall limit of the budget

# expect warning to occur if transaction is more than current category limit

# expect warning to occur if transaction is more than overall limit
end
